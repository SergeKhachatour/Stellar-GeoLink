{
  "name": "stellar-geolink",
  "version": "1.0.0",
  "description": "Stellar GeoLink - Location-based NFT platform",
  "main": "backend/app.js",
  "scripts": {
    "start": "cd backend && npm install --production && node app.js",
    "install": "cd backend && npm install --production",
    "build": "cd frontend && npm run build && cd ../backend && node ../deploy-frontend-to-backend.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "stellar",
    "nft",
    "location",
    "blockchain"
  ],
  "author": "Stellar GeoLink Team",
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^14.4.2"
  }
}
